[pytest]
testpaths = tests
pythonpath = . tests
markers =
    llm: 실제 LLM(Claude Code)/외부 API 를 호출하는 테스트 (기본 실행에서 제외)
    obsidian: 실행 중인 Obsidian(MCP 플러그인)이 필요한 테스트 (기본 실행에서 제외)
addopts = -m "not llm and not obsidian" -p no:cacheprovider
